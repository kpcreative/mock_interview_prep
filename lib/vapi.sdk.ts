import Vapi from '@vapi-ai/web';

//now we will create new instance of vapi like this
export const vapi= new Vapi(process.env. NEXT_PUBLIC_VAPI_WEB_TOKEN!);//! ye isliye as we are correctly and 100% sure specifying it ki it does exist in this env variable 